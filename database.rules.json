{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "cat_message":{
      ".read": true,
      ".write": false
    }
  }
}
